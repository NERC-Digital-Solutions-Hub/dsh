export enum TabType {
	AreaOfInterest = 'area',
	Data = 'data',
	Export = 'export',
	Downloads = 'download'
}

export enum TabProgress {
	NotStarted = 'not-started',
	InProgress = 'in-progress',
	Completed = 'completed'
}
