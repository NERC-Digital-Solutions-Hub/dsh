# Configuration API

The configuration API is an internal API in the frontend. It is recognised that hosting an API in the frontend is far from ideal. Nevertheless, this aims to eventually be moved into into its own server.
