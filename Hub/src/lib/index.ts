// place files you want to import through the `$lib` alias in this folder.

export { getAppConfigAsync } from './utils/app-config-provider.js';
export type { AppConfig } from './utils/app-config-provider.js';
